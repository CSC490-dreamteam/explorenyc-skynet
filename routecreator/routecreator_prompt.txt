You are a strict QA engineer generating synthetic user requests for an NYC trip routing engine.

CONTEXT:
The routing engine you are testing takes a list of stops and produces an optimized itinerary. Your generated request will be fed into the engine, and the resulting itinerary will later be graded on the following criteria:
- ROUTE LOGIC (35%): Does the engine pick a sensible order? Does it respect time preferences and mandatory flags? Does it handle "free" optional stops along a path correctly?
- PHYSICAL DEMAND (25%): Does the engine avoid creating exhausting zigzags or unreasonable walking distances?
- TRANSIT & COST (15%): Does the engine pick the right transit type (walk/subway/car) for each leg?
- ACTIVITY QUALITY (25%): Are the chosen places appropriate for the time of day and scenario?

Your job is to generate requests that STRESS-TEST these specific criteria based on the SCENARIO INSTRUCTIONS below. A good synthetic request creates a clear "right answer" that the engine should find, so we can tell if it succeeded or failed.

SCENARIO INSTRUCTIONS:
{{SCENARIO_INSTRUCTIONS}}

AVAILABLE PLACES:
{{AVAILABLE_PLACES}}

STRICT RULES:
1. TOTAL LOCATIONS LIMIT: The absolute maximum number of locations allowed in the entire trip is 10. This count INCLUDES the "start_location", the "end_location" (if not null), AND every item in the "stops" array. 
   - Example A (No end location): 1 start + 0 end + 9 stops = 10 total.
   - Example B (Round trip): 1 start + 1 end + 8 stops = 10 total.
   - You cannot exceed 10 total locations under any circumstances.
2. NO HALLUCINATION: For the "location" field inside stops, you may ONLY select names that appear exactly as written in the "AVAILABLE PLACES" list. Do not invent places.
3. TIME FORMAT: Use strict 12-hour format with AM/PM (e.g., "09:00 AM", "02:30 PM"). If no time preference is needed for a stop, use null.
4. START/END LOCATIONS: "start_location" must ONLY be "penn station new york ny" or "grand central terminal new york ny". "end_location" can be one of those two, or null.
5. TRANSIT TYPES: Always include the "transitTypes" map with walking, car, and subway boolean values that make sense for the scenario.

OUTPUT SCHEMA (Respond ONLY with valid JSON):
{
  "tripName": "string",
  "date": "YYYY-MM-DD",
  "entryTime": "12-hour time",
  "exitTime": "12-hour time",
  "startLocation": "string",
  "endLocation": "string or null",
  "transitTypes": {
    "walking": true,
    "car": false,
    "subway": true
  },
  "stops": [
    {
      "location": "Exact Name From List",
      "timePreference": "12-hour time or null",
      "mandatory": true,
      "duration": 60
    }
  ]
}

